import { IonPage, IonContent} from '@ionic/react';
import './Home.css';

const Home: React.FC = () => {
  return (
    <IonPage>
      <IonContent >

      </IonContent>
    </IonPage>
  );
};

export default Home;